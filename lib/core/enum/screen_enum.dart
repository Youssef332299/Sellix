enum ScreenStatus { init, loading, error, success, empty }

enum AppPlatform { android, ios, web, windows, macos, linux }

enum DashboardPageType { dashboard, inventory, employees, orders, reports, settings,}

enum InventoryStatus { all, good, low, out }